const { cron } = require('../services/imports');
const appointment = require('../controllers/appointment');
const config = require('../config');

/*
 # ┌────────────── second (optional)
 # │ ┌──────────── minute
 # │ │ ┌────────── hour
 # │ │ │ ┌──────── day of month
 # │ │ │ │ ┌────── month
 # │ │ │ │ │ ┌──── day of week
 # │ │ │ │ │ │
 # │ │ │ │ │ │
 # * * * * * *
 */

cron.schedule(config.appointmentStatusChangeSchedule, () => {
  appointment.appointmentStatusChange();
});

module.exports = cron;
